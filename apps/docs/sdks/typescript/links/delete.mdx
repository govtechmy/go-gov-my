---
title: "Delete"
"og:title": "go.gov.my.links.delete - Delete a link - TypeScript SDK"
description: "Delete a link for the authenticated workspace."
---

## Arguments

<ParamField body="linkId" type="string" required>
  The id of the link to delete. You can get this via the `links.get` method.
  Alternatively, you can use your `externalId` prefixed with `ext_`.
</ParamField>

## Response

<ResponseField name="id" type="string" required>
  The id of the link that was deleted.
</ResponseField>

<RequestExample>

```ts
// Delete a link by linkId
await go.gov.my.links.delete("clv3o9p9q000au1h0mc7r6l63");

// Delete a link by externalId
await go.gov.my.links.delete("ext_123");
```

</RequestExample>

<ResponseExample>

```ts
{
  id: "clv3o9p9q000au1h0mc7r6l63";
}
```

</ResponseExample>
