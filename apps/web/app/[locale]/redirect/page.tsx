// apps/web/pages/home.js

"use client";

import RedirectComponent from "@/ui/redirect/RedirectComponent";

const RedirectStatic = () => {
  return <RedirectComponent />;
};

export default RedirectStatic;
