apiVersion: apps/v1
kind: Deployment
metadata:
  name: cron
spec:
  template:
    spec:
      containers:
        - name: cron
          env:
            - name: WEB_BASE
              value: http://host.docker.internal:8888